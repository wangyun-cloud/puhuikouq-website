## Codebase Patterns
- `next.config.mjs` with `images: { unoptimized: true }` makes individual `unoptimized` props redundant; still add `sizes` and `loading` props for better semantics
- Add `fetchPriority="high"` to hero images alongside `priority` prop for optimal LCP
- Preconnect to `https://cdn.sanity.io` in layout `<head>` to improve Sanity image loading performance
- Use system font stack (PingFang SC, Microsoft YaHei) instead of next/font/google to avoid network failures during static export builds
- shadcn CLI may install v4-style components on Next.js 14; ensure globals.css uses standard Tailwind v3 `@tailwind` directives
- Custom colors defined as CSS variables in globals.css and mapped in tailwind.config.ts
- Sanity Studio should be a separate package (`studio/`) because Sanity v5 requires React 19, which conflicts with Next.js 14's React 18
- Sanity project ID can be configured via `SANITY_STUDIO_PROJECT_ID` env var in `sanity.config.ts` and `sanity.cli.ts`
- Sanity schemas use `defineType` and `defineField` from `sanity` for type-safe schema definitions
- Sanity `createClient` throws if `projectId` is empty; initialize the client LAZILY in `lib/sanity.ts` to keep builds green before a project ID is configured
- `@base-ui/react` components use `render` prop for composition, NOT `asChild` like Radix (e.g., `<SheetTrigger render={<Button />} />`)
- `@base-ui/react` Accordion uses `multiple` boolean prop, NOT `type="multiple"` like Radix
- Next.js 14 App Router natively supports `robots.ts` and `sitemap.ts` for static export—no extra library needed
- Server Actions are NOT supported with `output: 'export'` in Next.js 14; use API routes + client-side `fetch` for form submissions

## 2026/04/13 17:17 - US-001
- Initialized Next.js 14 project in `website/` with TypeScript, Tailwind CSS v3.4, App Router
- Configured ESLint + Prettier with Tailwind plugin
- Configured `next.config.mjs` for static export (`output: 'export'`, `distDir: 'dist'`)
- Installed shadcn/ui with base components: Button, Card, Sheet, Dialog
- Defined medical blue + warm beige design tokens in `globals.css`
- Typecheck passes, build succeeds
- Files changed: `website/` (entire project scaffold), `website/CLAUDE.md`
- **Learnings for future iterations:**
  - `create-next-app` creates a nested `.git` directory which must be removed before committing to the main repo
  - When using static export, avoid `next/font/google` due to build-time network font downloads
  - The `website/.gitignore` should exclude `dist/` (build output)
---

## 2026/04/13 18:03 - US-002
- Initialized Sanity v3 studio in `studio/` with React 19, Sanity 5.20, `@sanity/vision`
- Created `sanity.config.ts`, `sanity.cli.ts`, `tsconfig.json`, and package scripts
- Defined content schemas: `article`, `doctor`, `clinic`, `booking`, `banner` in `studio/schemas/documents/`
- Build passes (`npm run build` succeeds) and dev server starts successfully on port 3334
- Sanity Studio displays all 5 content types in the structure tool
- Added sample clinic data file at `studio/sample-data/clinic-sample.ndjson` ready for import
- TypeScript typechecking passes for all schema files
- Files changed: `studio/` (entire CMS scaffold), `studio/CLAUDE.md`
- **Learnings for future iterations:**
  - Sanity v5 requires React 19, so the studio must be a separate package from the Next.js 14 frontend
  - `sanity init --project-name` requires `--organization <id>` in unattended mode; manual scaffolding is more reliable for automation
  - The studio can start with a placeholder `projectId` to verify schemas, but actual data reads/writes require a real Sanity project ID
  - Booking schema includes a `status` field (pending/confirmed/completed/cancelled) for the admin view in US-010
  - Article schema includes a `quickAnswer` field (40-80 chars) for GEO optimization in US-011
  - Clinic schema has an `order` field to control display sequence on the contact page
  - Use `defineType` + `defineField` throughout for strong typing and editor support
---

## 2026/04/13 19:15 - US-003
- Installed `@sanity/client`, `@sanity/image-url`, and `next-sitemap` in `website/`
- Created `src/lib/sanity.ts` with lazy client initialization to avoid build errors when project ID is not configured
- Updated `RootLayout` with semantic HTML (`header`, `main`, `footer`), comprehensive metadata, OG/Twitter cards, and viewport theme colors
- Created `Navigation` component with desktop nav, mobile Sheet menu, phone CTA, and booking button
- Created `Footer` component with brand info, quick links, services, legal section, and ICP placeholder
- Added `robots.ts` (allowing all crawlers) and `sitemap.ts` with all planned routes
- Created `/sanity-test` page to verify Sanity data fetching with graceful fallback when unconfigured
- Updated `Button` component to support `asChild` via `@radix-ui/react-slot`
- Typecheck passes, lint passes, build succeeds, static export verified in `dist/`
- Files changed: `website/src/lib/sanity.ts`, `website/src/components/navigation.tsx`, `website/src/components/footer.tsx`, `website/src/app/layout.tsx`, `website/src/app/robots.ts`, `website/src/app/sitemap.ts`, `website/src/app/sanity-test/page.tsx`, `website/src/components/ui/button.tsx`, `website/package.json`, `website/CLAUDE.md`
- **Learnings for future iterations:**
  - `@base-ui/react` components use `render` prop for composition, not `asChild` like Radix (e.g., `<SheetTrigger render={<Button />} />`)
  - Sanity `createClient` throws if `projectId` is empty, so the client MUST be initialized lazily in `lib/sanity.ts` to keep builds green before a project ID is set
  - Next.js 14 App Router natively supports `robots.ts` and `sitemap.ts`—no need for `next-sitemap` for basic static exports
  - Navigation/footer patterns use the medical blue (`text-primary`) + warm beige (`bg-background`) palette consistently
---

## 2026/04/13 19:45 - US-004
- Developed full homepage with banner carousel, institutional introduction, service category grid, doctor team preview, and contact/CTA section
- Created `BannerCarousel` client component in `website/src/components/home/banner-carousel.tsx` with auto-rotation, prev/next controls, and dot indicators
- Homepage fetches banners, doctors, and clinics from Sanity with graceful fallback data when unconfigured
- Service grid includes 5 categories: 种植牙, 牙齿矫正, 牙齿修复, 牙周治疗, 儿童口腔
- Doctor preview displays up to 4 doctors with photo, name, specialties, and years of practice
- Added LocalBusiness Schema.org JSON-LD structured data with address, phone, and opening hours
- Booking CTA buttons link to `/booking` and phone number
- Typecheck passes, build succeeds, static export verified
- Files changed: `website/src/app/page.tsx`, `website/src/components/home/banner-carousel.tsx`
- **Learnings for future iterations:**
  - When Sanity images may be missing, use conditional rendering with CSS gradient fallbacks instead of broken `<Image>` placeholders
  - The ` medicinal-blue to medical-blue-light` gradient serves as a clean fallback for both banners and doctor photos
  - Keep banner carousel as a client component (state for current slide) while the page remains a server component fetching Sanity data
  - For Schema.org in Next.js 14 App Router, inject JSON-LD via `<script type="application/ld+json">` directly in the RSC
---

## 2026/04/13 20:08 - US-005
- Created reusable `ServicePageTemplate` component in `website/src/components/services/service-template.tsx`
- Template includes sections: hero banner, What is XXX, Applicable situations, Treatment process, Precautions, FAQ accordion, and booking CTA
- Developed Implant page (`/services/implant`) with objective descriptions and FAQPage Schema.org markup
- Developed Orthodontics page (`/services/orthodontics`) with objective descriptions and FAQPage Schema.org markup
- Installed `@base-ui/react` Accordion component for FAQ sections
- All content avoids absolute terms and efficacy claims; uses cautious, objective language
- Typecheck passes, lint passes, build succeeds, static export verified in `dist/`
- Files changed: `website/src/components/services/service-template.tsx`, `website/src/app/services/implant/page.tsx`, `website/src/app/services/orthodontics/page.tsx`, `website/src/components/ui/accordion.tsx`
- **Learnings for future iterations:**
  - `@base-ui/react` Accordion uses `multiple` boolean prop, NOT `type="multiple"` like Radix
  - Service pages follow a consistent template pattern: hero → what is → applicable → process → precautions → FAQ → CTA
  - FAQPage Schema is injected per-page via JSON-LD in the RSC, using the same FAQ data that renders the accordion
  - Treatment process timeline uses a mobile-first alternating layout with a central vertical line on desktop
---

## 2026/04/13 11:22 - US-006
- Created Restoration page (`/services/restoration`) with FAQPage Schema.org markup and objective descriptions
- Created Periodontal page (`/services/periodontal`) with FAQPage Schema.org markup and objective descriptions
- Created Pediatric page (`/services/pediatric`) with FAQPage Schema.org markup and objective descriptions
- Created Doctors page (`/doctors`) displaying cards with photo, name, education, qualifications, specialties, years of practice; includes Physician Schema.org markup
- Created Contact page (`/contact`) displaying all 4 clinics with address, phone, hours, and map links; includes fallback data when Sanity is unconfigured
- All pages use fallback data patterns consistent with existing pages
- TypeScript typecheck passes, lint passes, static export build succeeds with 15 pages
- Files changed: `website/src/app/services/restoration/page.tsx`, `website/src/app/services/periodontal/page.tsx`, `website/src/app/services/pediatric/page.tsx`, `website/src/app/doctors/page.tsx`, `website/src/app/contact/page.tsx`
- **Learnings for future iterations:**
  - Physician Schema.org markup uses `@type: "Physician"` with `medicalSpecialty`, `worksFor`, `alumniOf`, and `hasCredential` fields
  - Doctors page follows the same card grid pattern as the homepage doctor preview but with expanded details (education, qualifications)
  - Contact page uses a 16:9 aspect ratio placeholder for clinic images with MapPin icon fallback
  - All service pages reuse the `ServicePageTemplate` component; only the data object and metadata differ
---

## 2026/04/13 12:35 - US-007
- Created knowledge article list page (`/knowledge`) with category filtering and ItemList Schema.org markup
- Created knowledge article detail page (`/knowledge/[slug]`) with Portable Text rendering, quick answer GEO optimization, related articles, and Article Schema.org markup
- Installed `@portabletext/react` for rendering Sanity Portable Text content with custom components for headings, lists, marks, and images
- Created client-side `KnowledgeList` component for category filtering because `searchParams` cannot be used in server components during static export
- Added 5 fallback articles with realistic portable text content for testing when Sanity is unconfigured
- Updated `generateStaticParams` in detail page to produce static paths from both Sanity data and fallback articles
- Typecheck passes, lint passes, static export build succeeds with 21 pages
- Files changed: `website/src/app/knowledge/page.tsx`, `website/src/app/knowledge/knowledge-list.tsx`, `website/src/app/knowledge/[slug]/page.tsx`, `website/src/components/portable-text.tsx`, `website/package.json`
- **Learnings for future iterations:**
  - In Next.js 14 static export, `searchParams` cannot be accessed in server components. Use a client component with `useSearchParams` wrapped in `<Suspense>`
  - `@portabletext/react` warns about unknown block styles like `bullet` and `number` even though they're handled by `list`/`listItem`; add them to `components.block` to silence warnings
  - Sanity article references in GROQ can be dereferenced with `relatedArticles[]->` to fetch full related article documents
  - The `generateStaticParams` function should handle both Sanity-configured and unconfigured states to keep builds green
---

## 2026/04/13 12:50 - US-008
- Created 10 comprehensive educational dental articles covering all 5 categories (implant ×2, orthodontics ×2, oral-care ×2, periodontal ×2, pediatric ×2)
- Each article includes 3-5 Q&A style subheadings, authoritative references (WHO, 中华口腔医学会, 第四次全国口腔健康流行病学调查), and objective, cautious language
- Extracted fallback article data into shared `website/src/lib/article-content.ts` to keep page files maintainable
- Articles are 2000-4000 Chinese characters each (equivalent to 1500-2500 words)
- Generated `studio/sample-data/articles-10.ndjson` ready for Sanity import
- Updated `knowledge/page.tsx` and `knowledge/[slug]/page.tsx` to use shared fallback data
- Typecheck passes, lint passes, static export build succeeds with 26 pages including all 10 article detail pages
- Verified generated HTML contains all article titles and content correctly
- Files changed: `website/src/lib/article-content.ts`, `website/src/app/knowledge/page.tsx`, `website/src/app/knowledge/[slug]/page.tsx`, `studio/sample-data/articles-10.ndjson`, `website/CLAUDE.md`
- **Learnings for future iterations:**
  - Large fallback datasets should be extracted to shared library files (e.g., `src/lib/article-content.ts`) instead of being duplicated inline in page components
  - When writing Chinese content with quotes inside TypeScript strings, use 「」or '' instead of " to avoid premature string termination
  - Sanity NDJSON can be generated programmatically from the same fallback source to ensure content consistency between website and CMS
  - The `generateStaticParams` function handles all 10 article slugs automatically from the shared fallback data
---

## 2026/04/13 13:30 - US-009
- Created booking form page at `/booking` with fields: name, phone, service, preferred date, symptoms, clinic selection
- Implemented client-side form validation (name length, phone regex, service required, symptoms max 500 chars)
- Created API route at `/api/booking` to create Booking documents in Sanity via server-side write client
- Added `getSanityWriteClient()` in `lib/sanity.ts` using `SANITY_API_WRITE_TOKEN` for authenticated mutations
- Created guide page at `/guide` with appointment process timeline, business hours, and transportation info
- Created privacy policy page at `/privacy` compliant with personal information protection law (PIPL)
- Updated `website/CLAUDE.md` with form/static-export patterns
- Typecheck passes, lint passes, static export build succeeds with 30 pages
- Files changed: `website/src/lib/sanity.ts`, `website/src/app/api/booking/route.ts`, `website/src/app/booking/page.tsx`, `website/src/app/booking/booking-form.tsx`, `website/src/app/guide/page.tsx`, `website/src/app/privacy/page.tsx`, `website/CLAUDE.md`
- **Learnings for future iterations:**
  - Server Actions are NOT supported with `output: 'export'` in Next.js 14; use API routes + client-side `fetch` instead
  - Keep Sanity write tokens server-side in API routes, never expose them in client bundles
  - `useActionState` is React 19 only; on React 18 use `useFormState` + `useFormStatus` from `react-dom`
  - For Chinese privacy policies, reference 《中华人民共和国个人信息保护法》explicitly for compliance credibility
---

## 2026/04/13 21:50 - US-010
- Created custom desk structure in `studio/structure/index.ts` with dedicated `预约管理` (Booking Management) section
- Added filtered sub-views for all booking statuses: 全部预约 (all), 待处理 (pending), 已确认 (confirmed), 已完成 (completed), 已取消 (cancelled)
- Configured default ordering by `submittedAt` descending so newest bookings appear first
- Updated `booking` schema preview to include `clinic.name` reference, showing all key fields: name, phone, service, clinic, status, submittedAt
- Updated `sanity.config.ts` to use the custom structure builder
- Studio build passes successfully; dev server starts and serves the admin view
- Files changed: `studio/structure/index.ts`, `studio/sanity.config.ts`, `studio/schemas/documents/booking.ts`, `studio/CLAUDE.md`
- **Learnings for future iterations:**
  - Sanity v3 custom desk structures are configured via `structureTool({ structure })` in `sanity.config.ts`
  - Use `S.documentTypeList('booking').filter('_type == "booking" && status == "pending"')` for filtered admin views
  - Exclude default document type list items with `S.documentTypeListItems().filter((item) => item.getId() !== 'booking')`
  - Reference fields can be dereferenced in preview `select` using dotted paths like `clinic.name`
  - `defaultOrdering([{ field: 'submittedAt', direction: 'desc' }])` ensures the most recent items appear first in admin lists
---

## 2026/04/13 22:45 - US-011
- Created `website/public/llms.txt` with comprehensive site content overview for LLM crawlers
- Verified all 5 service pages use FAQPage Schema markup via `ServicePageTemplate`
- Verified all article detail pages use Article Schema markup and list page uses ItemList Schema
- Optimized images: added `fetchPriority="high"` to first banner image, `loading="lazy"` and `sizes` props to below-fold images (doctors, clinics, article images)
- Added `<link rel="preconnect">` and `<link rel="dns-prefetch">` for `cdn.sanity.io` in `layout.tsx`
- Removed redundant `unoptimized` props from individual `<Image>` components since `next.config.mjs` already sets `images.unoptimized: true`
- Static export build succeeds with all 30 pages pre-rendered; typecheck and lint pass
- Files changed: `website/public/llms.txt`, `website/src/components/home/banner-carousel.tsx`, `website/src/app/page.tsx`, `website/src/app/doctors/page.tsx`, `website/src/app/contact/page.tsx`, `website/src/components/portable-text.tsx`, `website/src/app/layout.tsx`, `prd.json`
- **Learnings for future iterations:**
  - Even with `images.unoptimized: true` globally, adding `sizes` and `loading` props to `<Image>` improves Lighthouse semantics and accessibility scores
  - `fetchPriority` prop on Next.js Image complements `priority` for better LCP on hero images
  - Preconnecting to the Sanity CDN in layout `<head>` is a simple but effective performance win for sites using Sanity images
---

## 2026/04/13 20:20 - US-012
- Completed site-wide compliance review for absolute terms, efficacy claims, testimonials, and before/after images
- Verified no prohibited absolute terms (最佳, 根治, 专家, 权威, etc.) or efficacy claims (成功率 99%, etc.) in content
- Confirmed no patient testimonials or before/after comparison images exist in the project
- Verified institutional name `上海普惠口腔` is consistent across all pages and metadata
- Footer already contains ICP备案号 placeholder: `沪ICP备XXXXXXXX号-1`
- Added explicit `export const metadata` to homepage (`website/src/app/page.tsx`) and sanity-test page (`website/src/app/sanity-test/page.tsx`)
- Fixed mixed English/Chinese text on homepage: `patient experience` → `患者就诊体验`
- TypeScript (`npx tsc --noEmit`) passes with zero errors
- ESLint (`npm run lint`) passes with zero warnings
- Static export build succeeds with all 30 pages pre-rendered and zero errors
- Files changed: `website/src/app/page.tsx`, `website/src/app/sanity-test/page.tsx`, `prd.json`, `progress.txt`
- **Learnings for future iterations:**
  - Even when a layout provides default metadata, every `page.tsx` should export explicit metadata for compliance and SEO clarity
  - Use `grep -r` with Chinese absolute term patterns for quick compliance sweeps before delivery
  - Mixed-language copy (e.g., English words in Chinese content) is easy to miss and should be checked during final review
  - The banner carousel and doctor cards already handle missing Sanity images with CSS gradient fallbacks, so no 404 console errors occur from placeholder image paths
---

